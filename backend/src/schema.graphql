#import * from './generated/prisma.graphql'

type Mutation {
  createVideo(title: String!, description: String, category: String!): Video!
  updateVideo(
    id: ID!
    title: String
    description: String
    category: String
  ): Video!
  deleteVideo(id: ID!): Video
  createCategory(name: String!): Category!
  updateCategory(id: ID!, name: String!): Video!
  deleteCategory(id: ID!): Category
  createComVideo(comment: String!): ComVideo!
  updateComVideo(id: ID!, comment: String!): ComVideo!
  deleteComVideo(id: ID!): ComVideo
  signup(email: String!, password: String!, name: String!): User!
}

type Query {
  # querys sellect all
  videos: [Video]!
  categorys: [Category]!
  comVideos: [ComVideo]!
  users: [User]!
  # querys selecionar por id
  video(where: VideoWhereUniqueInput!): Video
  category(where: CategoryWhereUniqueInput!): Category
  # querys selecionar a comparar com id de outra tabela(users) ( select geral)
  videosUser(id: ID!): [Video]
  comVideo(id: ID!): [ComVideo]

  me: User
}
